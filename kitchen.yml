---
driver:
  name: vagrant

provisioner:
  name: shell

verifier:
  name: inspec

platforms:
  - name: tf-plugin-bionic
    driver:
      box: tf-plugin-bionic
      provider: virtualbox

suites:
  - name: default
    verifier:
      inspec_tests:
        - test/integration/default
    attributes:
